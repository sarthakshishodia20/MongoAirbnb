"use strict";
/* eslint-disable @typescript-eslint/no-explicit-any */
/**
 * @author shaosong
 * @description cookies
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.cookies = void 0;
function getCookieStr() {
    return document.cookie.split(";");
}
/**
 * 获取所有cookie
 * @returns any[]
 */
function getCookieList() {
    var _a;
    const cookies = [];
    (_a = getCookieStr()) === null || _a === void 0 ? void 0 : _a.forEach((item) => {
        const [key, value] = item.trim().split("=");
        cookies.push({ [key]: value });
    });
    return cookies;
}
/**
 * 获取localStorage
 * @param key 对应key值
 * @returns 结果
 */
const getCookie = (key) => {
    var _a;
    const data = (_a = getCookieStr()) === null || _a === void 0 ? void 0 : _a.find((item) => item.includes(`${key}=`));
    if (!data)
        return undefined;
    const [key1, value] = data.trim().split("=");
    console.log(key1);
    return value;
};
/**
 * 设置单个localStorage
 * @param key 对应key值
 * @param value 存储值
 */
const setCookie = (key, value) => {
    document.cookie = `${key}=${value}`;
};
/**
 * 移除localStorage
 * @param key 对应key值
 */
const removeCookie = (key) => {
    document.cookie = `${key}=;path=/;max-age=0`;
};
/**
 * 清除所有localStorage
 */
const clearCookie = () => {
    getCookieStr().map((item) => {
        const [key] = item.trim().split("=");
        removeCookie(key);
    });
};
exports.cookies = {
    getCookieList,
    getCookie,
    setCookie,
    removeCookie,
    clearCookie,
};
